#include "main"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiple
 * @y: The second number to multiple with first number
 * Return: The result of multiplication between x and y
 */

int mul(int x, int y)
{
return (x * y);
}
